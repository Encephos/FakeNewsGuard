# Dev / Test Abhängigkeiten
-r requirements.txt

pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.12.0
respx>=0.21.0          # HTTP-Mocking für httpx
freezegun>=1.4.0       # Zeit-Mocking (für Cache-TTL Tests)
